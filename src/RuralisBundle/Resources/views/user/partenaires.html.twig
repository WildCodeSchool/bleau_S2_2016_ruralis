{% extends 'base.html.twig' %}
{% block body %}
        <div class="container partenaire">
            <div class="row partenaireTop">
                <div class="col s12">
                    <h1>
                        Nos partenaires
                    </h1>
                </div>
                <div class="col s12">
                    <p>
                        Ils nous font déjà confiance !
                    </p>
                    <p>
                        L’association APRIORI, tout en maintenant son autonomie, accepte de mettre en commun les forces de ses partenaires pour encourager ce projet éditorial.
                    </p>
                    <p>
                        C’est une alliance de raison mais pas que … les relations devenant plus étroites, Ruralis Magazine exprime des attentes, nos partenaires nous aident à y répondre.
                    </p>
                </div>
            </div>
            <div class="row">
                {% for partenaire in partenaires %}
                    <div class="col m3 s12">
                        <div class="row">
                            <div class="col offset-s1 s10 nomPart">
                                <p>{{ partenaire.nom }}</p>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col s12 valign-wrapper photoPart">
                                <img src="{{ asset('/uploads/images/') }}{{ partenaire.image.url }}" alt="{{ partenaire.image.alt }}">
                            </div>
                        </div>
                        <div class="row">
                            <div class="col offset-s1 s10 descPart">
                                <p>{{ partenaire.descriptif }}</p>
                            </div>
                        </div>

                    </div>

                {% endfor %}
            </div>
        </div>

{% endblock body %}